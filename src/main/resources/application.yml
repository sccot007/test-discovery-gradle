server:
  port: 8761

spring:
  application:
    name: discoveryservice

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
